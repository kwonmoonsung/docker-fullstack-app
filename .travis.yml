language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t star992411/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true star992411/react-test-app npm run test

after_success:
  - docker build -t star992411/docker-frontend ./frontend
  - docker build -t star992411/docker-backend ./backend
  - docker build -t star992411/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push star992411/docker-frontend
  - docker push star992411/docker-backend
  - docker push star992411/docker-nginx